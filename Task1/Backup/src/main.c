#include "Backup.h"

int main(int argc, const char* argv[])
{
    if (argc != 3)
    {
        fprintf(stderr, "BAD ARGS!\nInput backup and storage folder!\n");
        return -1;
    }

    return 0;
}
